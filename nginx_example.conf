
# 这是一个 Nginx 配置示例
# 通常文件位于 /etc/nginx/sites-available/starbaba 或 /etc/nginx/conf.d/starbaba.conf

server {
    # 1. 监听 80 端口 (HTTP)
    listen 80;
    server_name yourdomain.com www.yourdomain.com; # 替换成你的域名

    # 强制跳转 HTTPS (可选，推荐)
    # return 301 https://$host$request_uri;

    location / {
        # 转发请求到本地的 3000 端口 (PM2 运行的服务)
        proxy_pass http://127.0.0.1:3000;
        
        # 必须的一些头信息设置，确保 Next.js 能获取正确信息
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# 2. 监听 443 端口 (HTTPS) - 需要 SSL 证书
# server {
#     listen 443 ssl;
#     server_name yourdomain.com;
#
#     ssl_certificate /path/to/your/certificate.crt;
#     ssl_certificate_key /path/to/your/private.key;
#
#     location / {
#         proxy_pass http://127.0.0.1:3000;
#         # ... (同上的 proxy 设置)
#     }
# }
